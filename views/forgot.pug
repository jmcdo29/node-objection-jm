extends layout
block content
  h1 Forgot your password
  p Please enter your email address so we can send you an email with steps on how to reset your password.
  if message && message.length != 0
    .info
      p= message
  form(action="/users/forgot", method="post")
    .custTable
      .row
        .cell.label
          | Email
        .cell.input 
          input(type="email", name="email")
    input(type="submit", value="Send Email")